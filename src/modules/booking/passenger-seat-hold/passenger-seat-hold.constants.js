'use strict';

const PASSENGER_SEAT_HOLD_DURATION_MS = 7 * 60 * 1000;
const PASSENGER_CONFIRMATION_PROCESSING_LEASE_MS = 2 * 60 * 1000;
const MAX_PASSENGER_SEATS_PER_BOOKING = 6;

module.exports = {
  PASSENGER_SEAT_HOLD_DURATION_MS,
  PASSENGER_CONFIRMATION_PROCESSING_LEASE_MS,
  MAX_PASSENGER_SEATS_PER_BOOKING,
};
